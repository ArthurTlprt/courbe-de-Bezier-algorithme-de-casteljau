function pts = interp_decasteljau(previousPts, ptsToKeep)
  if length(Pts) == 0
    pts = Pts;
  end
  pts = Pts;
  for i=1:length(Px)-1
    pts(end+1) = [(Px(i)+Px(i))/2, (Py(i)+Py(i))/2];
  end
end
